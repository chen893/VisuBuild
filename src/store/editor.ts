import { createSlice } from '@reduxjs/toolkit'

const editorSlice = createSlice({
  name: 'edit',
  initialState: {

  },
  reducers: {

  }
})

export const editAction = editorSlice.actions
export default editorSlice.reducer
